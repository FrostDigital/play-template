# Application dependencies

require:
    - play
    - play -> secure
    - postmark -> postmark


repositories:
    - frost:
        type: local
        artifact: ${application.path}/../play-[module]
        contains:
          - postmark